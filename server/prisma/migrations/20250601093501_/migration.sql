/*
  Warnings:

  - You are about to drop the column `oldBalance` on the `masterorder` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `masterorder` DROP COLUMN `oldBalance`;
